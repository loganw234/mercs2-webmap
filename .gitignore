__pycache__/
*.pyc

# build output -- regenerated by build.py / CI (Pages + the rolling release both build it fresh)
/dist/
mercs2-webmap.html

# generated by the terrain pipeline (build_all.ps1) -- rebuilt from committed data/*.log + heightmap.js
missing_data.txt
/ingame/Terrain.lua
/ingame/GapStream.lua

# NOTE: src/data/map-image.js and src/data/collectibles.js ARE committed on purpose -- they're generated
# but REQUIRED at build time (CI has no copy of the source map.jpg), same as a vendored asset.
